# VM Control Web App

A modern web application for controlling and monitoring virtual machines in Azure. Built with Nuxt 4, Vue 3, and deployed as an Azure Static Web App with serverless Azure Functions backend.

## 🎯 Features

- **Real-time VM Status Monitoring** – Check if your VM is running or stopped with automatic polling
- **One-Click VM Control** – Easily turn your VM on or off with visual feedback
- **Smart Polling** – Adaptive polling (normal vs. fast) for responsive UX
- **Error Tracking** – Automatic error logging to Azure Application Insights
- **Time-based Overrides** – VM start requests have a 4-hour automatic override window
- **Responsive Design** – Works on desktop and mobile devices
- **Type-Safe Backend** – Built with TypeScript for reliability

## 🏗️ Architecture

```text
┌─────────────────────┐
│   Frontend (Nuxt)   │
│  - Vue 3 SFC        │
│  - PrimeVue UI      │
│  - TailwindCSS      │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Azure Static App   │
│  - SPA Routing      │
│  - CDN Distribution │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Azure Functions API │
│  - getVmStatus      │
│  - manualVmPower    │
│  - logApi           │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│   Azure Services    │
│  - Compute (VMs)    │
│  - Identity (Auth)  │
│  - Tables (Storage) │
│  - App Insights     │
└─────────────────────┘
```

## 📋 Tech Stack

### Frontend

- **Nuxt 4** – Full-stack Vue framework
- **Vue 3** – Reactive UI components
- **PrimeVue** – Component library
- **TailwindCSS** – Utility-first CSS
- **Axios** – HTTP client
- **Pinia Colada** – Hooks for queries
- **TypeScript** – Type safety

### Backend

- **Azure Functions** (Node.js) – Serverless API
- **TypeScript** – Type-safe code
- **@azure/arm-compute** – VM management
- **@azure/data-tables** – VM override state
- **@azure/identity** – Azure auth
- **Application Insights** – Monitoring & logging

## 🚀 Getting Started

### Prerequisites

- **Node.js 20** (Latest LTS recommended)
- **npm** or **yarn**
- **Azure Functions Core Tools** (for local API development)
- **Azure CLI** (optional, for deployment)
- Azure subscription with:
  - Virtual Machine
  - Storage Account
  - Application Insights instance

### Installation

1. **Clone and install**

   ```bash
   git clone <repo-url>
   cd tboersma-dev-site
   npm install
   cd api && npm install && cd ..
   ```

2. **Set up environment variables**
   Fill in values for `local.settings.json` in `/api/`:

### Local Development

The dev command has been set up to build the api, start the dev server, and start the functions
You do need to run an additional command to host the local proxy server to redirect traffic to the azure functions

```bash
npm run dev

swa start http://localhost:3000 --api-location api/dist #in a different terminal
```

### Building for Production

```bash
# Build API
cd api && npm run build && cd ..

# Build frontend
npm run build
```

Output:

- Frontend: `.output/public/` (Static HTML/JS)
- Backend: `api/dist/` (Compiled Functions)

## 🔄 CI/CD

GitHub Actions workflow auto-generated by SWA:

- Builds Nuxt frontend
- Builds Azure Functions API
- Deploys to SWA
- Triggers on push to `main`

## 📚 Learn More

- [Nuxt Docs](https://nuxt.com)
- [Vue 3 Docs](https://vuejs.org)
- [Azure Functions Node.js Guide](https://learn.microsoft.com/en-us/azure/azure-functions/functions-reference-node)
- [Azure Static Web Apps](https://learn.microsoft.com/en-us/azure/static-web-apps/)
- [PrimeVue Components](https://primevue.org)
- [TailwindCSS](https://tailwindcss.com)
